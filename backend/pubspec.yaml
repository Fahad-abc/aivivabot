name: backend
description: AI Chatbot Backend Server
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_proxy: ^1.0.0
  shelf_cors_headers: ^0.1.5
  dotenv: ^4.2.0
  crypto: ^3.0.3
  logging: ^1.2.0
  http: ^1.2.0  # ✅ YEH ADD KARO
  pdf: ^3.10.4

dev_dependencies:
  test: ^1.24.0